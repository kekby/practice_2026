# 4. Наполнение интерфейса, отладка и стресс-тестирование

Запуск:
```bash
npm install
cp .env.example .env   # и подставить свои значения
node --env-file=.env server.js
```
Откроется на http://localhost:3000
